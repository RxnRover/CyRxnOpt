[metadata]
name = test_project
version = 0.1.0
author = zachcran

[options]
zip_safe = False
packages = find_namespace:
include_package_data = True
package_dir =
    =src

install_requires =
    numpy

[options.packages.find]
where = src
exclude =
    tests
